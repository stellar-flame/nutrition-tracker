export default function Index() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Nutrition Tracker (Local Dev)</h1>
      <p>We’ll add auth, forms, and API later. For now, hello world 👋</p>
    </main>
  );
}